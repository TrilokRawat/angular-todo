import { FormControl, FormGroup } from '@angular/forms';

export const todo = new FormGroup({
  title: new FormControl(''),
  description: new FormControl(''),
  thumbnail: new FormControl(''),
  links: new FormControl(''),
});



                                                                  

